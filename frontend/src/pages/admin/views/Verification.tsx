import { ComingSoon } from "@/pages/admin/components/adminUi";

/**
 * Admin verification view — identity verification queue.
 * Pending implementation.
 */
function Verification() {
  return (
    <ComingSoon
      title="Cola de verificación de identidad"
      description="23 documentos por revisar — mismo patrón que Usuarios."
    />
  );
}

export { Verification };
