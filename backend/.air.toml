root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/server"
  cmd = "go build -o ./tmp/server ./backend/cmd/server"
  delay = 500
  exclude_dir = ["tmp", "vendor", "node_modules", "frontend", "e2e", "docs", ".git", ".github"]
  exclude_regex = ["_test\\.go"]
  include_dir = ["backend"]
  include_ext = ["go", "toml", "yaml"]
  kill_delay = "500ms"
  send_interrupt = true
  stop_on_error = true

[log]
  time = false

[misc]
  clean_on_exit = true
